---
phase: 05-runtime-module-and-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-plugin/autonomous-peers-runtime.ts
  - gsd-plugin/orchestrator/orchestrator-helpers.ts
autonomous: true
requirements:
  - BRKR-04

must_haves:
  truths:
    - "buildExecutionWaves is importable directly from autonomous-peers-runtime.ts"
    - "waitForWaveComplete is importable directly from autonomous-peers-runtime.ts"
    - "All 29 existing orchestrator-helpers tests still pass via re-exports"
    - "No circular import between autonomous-peers-runtime.ts and orchestrator-helpers.ts"
  artifacts:
    - path: "gsd-plugin/autonomous-peers-runtime.ts"
      provides: "Standalone runtime module with Kahn's sort and wave polling"
      exports: ["buildExecutionWaves", "waitForWaveComplete", "pollOrchestratorMessages", "ackMessages", "sendStatusRequest", "reclaimExecutorTask", "PhaseNode"]
    - path: "gsd-plugin/orchestrator/orchestrator-helpers.ts"
      provides: "Re-exports from runtime module for backwards compatibility"
  key_links:
    - from: "gsd-plugin/orchestrator/orchestrator-helpers.ts"
      to: "gsd-plugin/autonomous-peers-runtime.ts"
      via: "re-export statements"
      pattern: "export.*from.*autonomous-peers-runtime"
    - from: "gsd-plugin/autonomous-peers-runtime.ts"
      to: "shared/types.ts"
      via: "type imports"
      pattern: "import type.*from.*shared/types"
---

<objective>
Extract the Kahn's algorithm topological sort (buildExecutionWaves) and wave polling loop (waitForWaveComplete) along with their runtime dependencies (pollOrchestratorMessages, ackMessages, sendStatusRequest, reclaimExecutorTask) from orchestrator-helpers.ts into a standalone autonomous-peers-runtime.ts module. Re-export from orchestrator-helpers.ts for backwards compatibility.

Purpose: Make the core scheduling algorithm independently testable without loading the full orchestrator context.
Output: gsd-plugin/autonomous-peers-runtime.ts (new), gsd-plugin/orchestrator/orchestrator-helpers.ts (modified with re-exports)
</objective>

<execution_context>
@/home/joshuaduffill/.claude/get-shit-done/workflows/execute-plan.md
@/home/joshuaduffill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-runtime-module-and-tests/05-CONTEXT.md
@.planning/phases/05-runtime-module-and-tests/05-RESEARCH.md

<interfaces>
<!-- From gsd-plugin/orchestrator/orchestrator-helpers.ts — functions to extract -->

export interface PhaseNode {
  number: number;
  name: string;
  dir: string;
  dependencies: number[];
  status: "pending" | "completed";
  filesModified: string[];
}

export function buildExecutionWaves(phases: PhaseNode[]): PhaseNode[][]
export async function pollOrchestratorMessages(myId: PeerId): Promise<{
  progresses: Array<{ msgId: number; payload: PhaseProgressPayload }>;
  completions: Array<{ msgId: number; payload: PhaseCompletePayload }>;
  blocks: Array<{ msgId: number; payload: PhaseBlockedPayload }>;
  statusResponses: Array<{ msgId: number; payload: StatusResponsePayload }>;
}>
export async function ackMessages(messageIds: number[]): Promise<void>
export async function sendStatusRequest(myId: PeerId, executorId: PeerId, taskId: number): Promise<void>
export async function reclaimExecutorTask(myId: PeerId, executorId: PeerId, taskId: number, waveId: number, reason: string): Promise<void>
export async function waitForWaveComplete(myId: PeerId, waveId: number, assignments: Map<number, PeerId>): Promise<{ completed: PhaseCompletePayload[]; blocked: PhaseBlockedPayload[]; reclaimed: number[] }>

<!-- From shared/types.ts — types needed by runtime module -->
import type {
  PeerId, PhaseCompletePayload, PhaseBlockedPayload,
  PhaseProgressPayload, StatusResponsePayload, ReclaimTaskPayload,
  Wave, TaskAssignment, PollMessagesResponse,
} from "../../shared/types.ts";
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create autonomous-peers-runtime.ts with extracted functions</name>
  <files>gsd-plugin/autonomous-peers-runtime.ts</files>
  <read_first>
    gsd-plugin/orchestrator/orchestrator-helpers.ts
    shared/types.ts
    .planning/phases/05-runtime-module-and-tests/05-RESEARCH.md
  </read_first>
  <action>
Create `gsd-plugin/autonomous-peers-runtime.ts` by extracting the following from `orchestrator-helpers.ts`:

1. **Duplicate `brokerFetch`** — Copy the `brokerFetch<T>` function verbatim, including the `BROKER_PORT` and `BROKER_URL` constants. This follows the established per-module self-contained pattern (do NOT import from another module).

2. **Move `PhaseNode` interface** — Cut the `PhaseNode` interface (lines 60-67 of orchestrator-helpers.ts) and paste into the new file. It has fields: `number`, `name`, `dir`, `dependencies`, `status`, `filesModified`.

3. **Move `buildExecutionWaves`** — Cut the entire function (lines 256-338 of orchestrator-helpers.ts). It takes `PhaseNode[]` and returns `PhaseNode[][]` using Kahn's algorithm. Paste verbatim, no modifications.

4. **Move `pollOrchestratorMessages`** — Cut the entire function (lines 517-548). It calls `brokerFetch<PollMessagesResponse>("/poll-messages", ...)` and categorizes messages by type.

5. **Move `ackMessages`** — Cut the entire function (lines 556-559). It calls `brokerFetch("/ack-message", ...)` with early return on empty array.

6. **Move `sendStatusRequest`** — Cut the entire function (lines 569-581). It sends a `status_request` message via `brokerFetch("/send-message", ...)`.

7. **Move `reclaimExecutorTask`** — Cut the entire function (lines 593-608). It sends `reclaim_task` message AND calls `/task-blocked`.

8. **Move `waitForWaveComplete`** — Cut the entire function (lines 725-815). It uses `pollOrchestratorMessages`, `ackMessages`, `sendStatusRequest`, and `reclaimExecutorTask` (all now local to this file).

**Type imports** at the top of the new file:
```typescript
import type {
  PeerId,
  PhaseCompletePayload,
  PhaseBlockedPayload,
  PhaseProgressPayload,
  StatusResponsePayload,
  ReclaimTaskPayload,
  Wave,
  TaskAssignment,
  PollMessagesResponse,
} from "../shared/types.ts";
```

**File header comment:**
```typescript
/**
 * autonomous-peers-runtime.ts
 *
 * Standalone runtime module: Kahn's topological sort (buildExecutionWaves)
 * and the wave polling loop (waitForWaveComplete).
 * Independently unit-testable without importing orchestrator-helpers.ts.
 *
 * NOTE: brokerFetch is intentionally duplicated per established per-module
 * self-contained pattern. Do not import from other helper modules.
 */
```

CRITICAL: Do NOT import anything from `orchestrator-helpers.ts`. The runtime module must have zero imports from sibling modules (only from `../shared/types.ts`). This prevents circular dependencies.
  </action>
  <verify>
    <automated>cd /home/joshuaduffill/dev/claude-peers-mcp && bun build gsd-plugin/autonomous-peers-runtime.ts --no-bundle 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File `gsd-plugin/autonomous-peers-runtime.ts` exists
    - Contains `export interface PhaseNode`
    - Contains `export function buildExecutionWaves`
    - Contains `export async function waitForWaveComplete`
    - Contains `export async function pollOrchestratorMessages`
    - Contains `export async function ackMessages`
    - Contains `export async function sendStatusRequest`
    - Contains `export async function reclaimExecutorTask`
    - Contains `const BROKER_PORT = process.env.CLAUDE_PEERS_PORT` (duplicated, not imported)
    - Contains `async function brokerFetch` (local, not imported)
    - Does NOT contain any `import.*from.*orchestrator-helpers`
  </acceptance_criteria>
  <done>autonomous-peers-runtime.ts exists with all 6 exported functions + PhaseNode interface, self-contained with local brokerFetch, no circular imports</done>
</task>

<task type="auto">
  <name>Task 2: Update orchestrator-helpers.ts with re-exports</name>
  <files>gsd-plugin/orchestrator/orchestrator-helpers.ts</files>
  <read_first>
    gsd-plugin/orchestrator/orchestrator-helpers.ts
    gsd-plugin/autonomous-peers-runtime.ts
    gsd-plugin/orchestrator/orchestrator-helpers.test.ts
  </read_first>
  <action>
Modify `orchestrator-helpers.ts` to re-export the extracted functions from the runtime module instead of defining them locally:

1. **Remove the moved functions and interface** — Delete the function bodies for: `PhaseNode` interface, `buildExecutionWaves`, `pollOrchestratorMessages`, `ackMessages`, `sendStatusRequest`, `reclaimExecutorTask`, `waitForWaveComplete`.

2. **Add re-export statement** — Near the top of the file (after the existing `export { sendDiscussChoice, waitForAnswer }` re-export on line 31), add:

```typescript
// Runtime module re-exports — these functions were extracted for independent testability.
export {
  buildExecutionWaves,
  waitForWaveComplete,
  pollOrchestratorMessages,
  ackMessages,
  sendStatusRequest,
  reclaimExecutorTask,
} from "../autonomous-peers-runtime.ts";
export type { PhaseNode } from "../autonomous-peers-runtime.ts";
```

3. **Keep all other functions** — `discoverPeers`, `parseRoadmapPhases`, `finalizePhaseNode`, `checkWaveConflicts`, `padPhase`, `dispatchWave`, `shouldDelegate`, `handleExecutorDeath`, `postWaveSync` remain in orchestrator-helpers.ts.

4. **Keep brokerFetch and constants** — The local `brokerFetch`, `BROKER_PORT`, `BROKER_URL` in orchestrator-helpers.ts must remain because the kept functions (`discoverPeers`, `checkWaveConflicts`, `dispatchWave`, `handleExecutorDeath`, `postWaveSync`) still use them.

5. **Update type imports** — The `import type` block at the top may need adjustment. Keep only the types that the remaining functions use. The types used exclusively by extracted functions (e.g., `PhaseProgressPayload`, `StatusResponsePayload`, `ReclaimTaskPayload`, `PollMessagesResponse`) can be removed from the import if no remaining function uses them. Check each type before removing.

6. **Run existing tests** to confirm backwards compatibility:
```bash
bun test gsd-plugin/orchestrator/orchestrator-helpers.test.ts
```

All 29 tests must pass. If any fail, the re-export is missing something — fix immediately.
  </action>
  <verify>
    <automated>cd /home/joshuaduffill/dev/claude-peers-mcp && bun test gsd-plugin/orchestrator/orchestrator-helpers.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `orchestrator-helpers.ts` contains `export { buildExecutionWaves` ... `} from "../autonomous-peers-runtime.ts"`
    - `orchestrator-helpers.ts` contains `export type { PhaseNode } from "../autonomous-peers-runtime.ts"`
    - `orchestrator-helpers.ts` does NOT contain `export function buildExecutionWaves` (function body removed)
    - `orchestrator-helpers.ts` does NOT contain `export async function waitForWaveComplete` (function body removed)
    - `orchestrator-helpers.ts` does NOT contain `export interface PhaseNode` (interface body removed)
    - `orchestrator-helpers.ts` still contains `export async function discoverPeers`
    - `orchestrator-helpers.ts` still contains `export function parseRoadmapPhases`
    - `orchestrator-helpers.ts` still contains `export function shouldDelegate`
    - All 29 orchestrator-helpers tests pass
  </acceptance_criteria>
  <done>orchestrator-helpers.ts re-exports 6 functions + PhaseNode type from runtime module, all 29 existing tests pass unchanged</done>
</task>

</tasks>

<verification>
1. `bun build gsd-plugin/autonomous-peers-runtime.ts --no-bundle` compiles without errors
2. `bun test gsd-plugin/orchestrator/orchestrator-helpers.test.ts` — all 29 tests pass
3. `bun test` — full test suite green (no regressions)
4. No circular import: `grep -r "from.*orchestrator-helpers" gsd-plugin/autonomous-peers-runtime.ts` returns nothing
</verification>

<success_criteria>
- autonomous-peers-runtime.ts exists with buildExecutionWaves, waitForWaveComplete, and 4 helper functions exported
- orchestrator-helpers.ts re-exports all moved identifiers
- All existing tests pass without modification
- No circular imports between the two modules
</success_criteria>

<output>
After completion, create `.planning/phases/05-runtime-module-and-tests/05-01-SUMMARY.md`
</output>
