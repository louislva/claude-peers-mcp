# Belt and braces: ignore everything in this directory.
# The bridge daemon reads creds from ~/.config/gsd-comms/telegram/telegram.env
# (outside the repo). This directory is a fallback for development convenience
# only — its contents must never be committed.
*
!.gitignore
